/**
 * Hookline TypeScript SDK — placeholder.
 */
export const VERSION = "0.0.1";

export function hello(): string {
  return `Hookline SDK v${VERSION}`;
}

// TODO: replace with real client API (REST + WebSocket helpers)